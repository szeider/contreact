# Task

Plan a 3-day weekend trip to Vienna, Austria.

Consider:
- Must-see attractions
- Local food recommendations
- Practical tips for visitors

Use the `think` tool to reason through your planning, then use `send_message` to share your recommendations with the operator. When you're done, use `stop` to end the session.
